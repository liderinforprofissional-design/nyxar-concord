<Window x:Class="NyxarConcord.Views.ScreenSharePicker"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Compartilhar tela" Height="580" Width="480"
        Background="{StaticResource Bg.Darkest}"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True">
    <Border CornerRadius="16" Background="{StaticResource Bg.Dark}" BorderBrush="{StaticResource Border.Soft}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Padding="24,20,24,8">
                <StackPanel>
                    <TextBlock Text="Compartilhar tela" FontSize="20" FontWeight="Bold"
                               Foreground="{StaticResource Text.Primary}" />
                    <TextBlock Text="Escolha uma tela ou uma janela de aplicativo" FontSize="12"
                               Foreground="{StaticResource Text.Muted}" Margin="0,2,0,0" />
                </StackPanel>
            </Border>

            <ListBox x:Name="SourceList" Grid.Row="1" Margin="12,4,12,0" Background="Transparent"
                     BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     SelectionChanged="SourceList_SelectionChanged">
                <ListBox.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" FontSize="11" FontWeight="SemiBold"
                                           Foreground="{StaticResource Text.Muted}" Margin="8,12,0,4" />
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ListBox.GroupStyle>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="10,8" />
                        <Setter Property="Margin" Value="4,2" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="b" Background="{StaticResource Bg.Card}" CornerRadius="8"
                                            Padding="{TemplateBinding Padding}" BorderThickness="1"
                                            BorderBrush="Transparent">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="b" Property="Background" Value="{StaticResource Bg.Hover}" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Accent}" />
                                            <Setter TargetName="b" Property="Background" Value="{StaticResource Bg.Hover}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid Width="30" Height="30" VerticalAlignment="Center">
                                <Image Source="{Binding Icon}" Width="26" Height="26"
                                       RenderOptions.BitmapScalingMode="HighQuality" />
                                <Path Data="{StaticResource Icon.Monitor}" Width="22" Height="22" Stretch="Uniform"
                                      Fill="{StaticResource Accent}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Visibility="{Binding Icon, Converter={StaticResource NullToVis}}" />
                            </Grid>
                            <TextBlock Grid.Column="1" Text="{Binding Title}" Foreground="{StaticResource Text.Primary}"
                                       FontSize="13" VerticalAlignment="Center" Margin="12,0,0,0"
                                       TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Grid.Row="2" Padding="24,14">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="Qualidade:" Foreground="{StaticResource Text.Muted}" FontSize="12"
                                   VerticalAlignment="Center" Margin="0,0,10,0" />
                        <RadioButton x:Name="R720" Content="720p" IsChecked="True" GroupName="res"
                                     Style="{StaticResource PillRadio}" />
                        <RadioButton x:Name="R480" Content="480p" GroupName="res" Style="{StaticResource PillRadio}" />
                        <RadioButton x:Name="R360" Content="360p" GroupName="res" Style="{StaticResource PillRadio}" />
                    </StackPanel>
                    <Grid>
                        <TextBlock x:Name="SelectedLabel" Text="Nada selecionado" VerticalAlignment="Center"
                                   Foreground="{StaticResource Text.Muted}" FontSize="12" TextTrimming="CharacterEllipsis"
                                   MaxWidth="200" HorizontalAlignment="Left" />
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Cancelar" Style="{StaticResource GhostButton}" Margin="0,0,10,0" Click="Cancel_Click" />
                            <Button Content="Compartilhar" Style="{StaticResource AccentButton}" Click="Share_Click" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
